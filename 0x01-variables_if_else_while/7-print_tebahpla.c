#include <stdio.h>
/**
 * main - Entry pont
 * Description: 'extracting lowercase in reverse'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int p;

	for (p = 25; p >= 0; p++)
	{
		putchar('a' + p);
	}
	putchar('\n');
	return (0);
}
