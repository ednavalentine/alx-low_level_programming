#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Prints all single digits without using char'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 0; i++)
	{
		putchar((i % 10) + '0');
	}
	putchar('\n');
	return (0);
}
